import React from 'react'
import ReactDOM from 'react-dom/client'
import TarotReader from './tarot_reader.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TarotReader />
  </React.StrictMode>,
)
